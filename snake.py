from Tkinter import *
import random
import time


WIDTH = 1200
HEIGHT = 800

tk = Tk() #Create the window
tk.title("snake") #Title
canvas = Canvas(tk, width = WIDTH, height =HEIGHT, bg ='white') #Create the window with the background
canvas.pack() #DUNNOW
tk.mainloop()
